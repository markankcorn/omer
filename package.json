{
  "name": "omercount",
  "version": "1.0.0",
  "private": true,
  "description": "Omer counting website - omercount.com",
  "scripts": {
    "build:meditations": "node scripts/build-meditations.js",
    "dev": "npm run build:meditations && wrangler dev",
    "deploy": "npm run build:meditations && wrangler deploy",
    "preview": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
